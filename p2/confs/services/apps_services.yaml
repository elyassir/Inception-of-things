apiVersion: v1
kind: Service
metadata:
  name: app-one-service
spec:
  selector:
    app: app-one
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
---

apiVersion: v1
kind: Service
metadata:
  name: app-two-service
spec:
  selector:
    app: app-two
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
---

apiVersion: v1
kind: Service
metadata:
  name: app-three-service
spec:
  selector:
    app: app-three
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP